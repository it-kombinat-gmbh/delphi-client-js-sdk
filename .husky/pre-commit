#[ -n "$CI" ] && exit 0

# Ensure nvm-managed Node is available when git is invoked from GUIs / VS Code.
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

pnpm exec lint-staged
